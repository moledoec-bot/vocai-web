'use client'

// TODO CP3: implementar hook con scroll RAF-throttle que controla:
//   - opacity del canvas cosmos (curva 1.0 → 0.40 → 0.08)
//   - parallax del .hero-copy (translateY × factor)
//   - estado .is-scrolled del navbar
//   - tilt sutil del .spline-placeholder (-2deg → +2deg)
//   - parallax leve de cards [data-parallax]
//   - flag window.__heroVisible para coordinar con el shader loop
// Por ahora un stub para que el módulo exista y los imports compilen.
export function useScrollDriven() {
  // Placeholder. La lógica real se monta en CP3 cuando el shader esté vivo.
}
